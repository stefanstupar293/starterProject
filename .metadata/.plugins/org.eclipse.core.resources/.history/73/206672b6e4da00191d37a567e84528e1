import {html, PolymerElement} from '@polymer/polymer/polymer-element.js';

/**
 * `login-test.html`
 *
 * LoginTest.html element.
 *
 * @customElement
 * @polymer
 */
class LoginTest.html extends PolymerElement {

    static get template() {
        return html`
            <style include="shared-styles">
                :host {
                    display: block;
                    height: 100%;
                }
            </style>
        `;
    }

    static get is() {
        return 'login-test.html';
    }

    static get properties() {
        return {
            // Declare your properties here.
        };
    }
}

customElements.define(LoginTest.html.is, LoginTest.html);
