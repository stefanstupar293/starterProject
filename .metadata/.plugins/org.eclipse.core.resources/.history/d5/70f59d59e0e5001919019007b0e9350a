import {html, PolymerElement} from '@polymer/polymer/polymer-element.js';

/**
 * `logout-test.java`
 *
 * LogoutTest.java element.
 *
 * @customElement
 * @polymer
 */
class LogoutTest.java extends PolymerElement {

    static get template() {
        return html`
            <style include="shared-styles">
                :host {
                    display: block;
                    height: 100%;
                }
            </style>
        `;
    }

    static get is() {
        return 'logout-test.java';
    }

    static get properties() {
        return {
            // Declare your properties here.
        };
    }
}

customElements.define(LogoutTest.java.is, LogoutTest.java);
