import {html, PolymerElement} from '@polymer/polymer/polymer-element.js';

/**
 * `login-bravo.html`
 *
 * LoginBravo.html element.
 *
 * @customElement
 * @polymer
 */
class LoginBravo.html extends PolymerElement {

    static get template() {
        return html`
            <style include="shared-styles">
                :host {
                    display: block;
                    height: 100%;
                }
            </style>
        `;
    }

    static get is() {
        return 'login-bravo.html';
    }

    static get properties() {
        return {
            // Declare your properties here.
        };
    }
}

customElements.define(LoginBravo.html.is, LoginBravo.html);
